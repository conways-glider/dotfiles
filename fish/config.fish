if status is-interactive
    # Commands to run in interactive sessions can go here
    abbr -a -g g git
    abbr -a -g d docker
    abbr -a -g k kubectl
    abbr -a -g p podman
    abbr -a -g z zellij
    abbr -a -g vi nvim
    abbr -a -g vim nvim
end
